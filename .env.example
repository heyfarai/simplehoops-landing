POSTMARK_API_TOKEN=
POSTMARK_FROM_EMAIL=do-not-reply@shuuk.ca
OPS_EMAIL=farai@shuuk.ca

# Local dev only — uncomment to use Mailpit instead of Postmark
# USE_MAILPIT=true
# MAILPIT_SMTP_HOST=localhost
# MAILPIT_SMTP_PORT=1025
